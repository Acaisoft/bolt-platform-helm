{{- if index .Values "cluster-issuer" "enabled" -}}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
{{- index .Values "cluster-issuer" "spec" | toYaml | nindent 2 }}
{{- end }}
